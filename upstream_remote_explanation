The "upstream" remote is usually used to as the main source for a project. It will be the one from which others will fork and pull changes down from. Additionally it is set as the default target of git push when the 'git push -u master origin' command is made.